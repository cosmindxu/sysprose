{
  "level": "L5",
  "title": "one bad declaration must not lose the other forty",
  "note": "One bad declaration among forty-one must not cost the other forty. It does not: all 41 elements survive and exactly one error is reported, naming the offending keyword. FIXED 2026-09-02: recovery used to re-home every declaration AFTER the fault to the root namespace; they are now back inside `package Big`, so the only surviving root is `Big`."
}
